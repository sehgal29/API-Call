# API-Call
A user management app that fetches data from an API and displays it as user cards. Each card has a Delete button to remove the user and a View Profile button to see full user information, including address, company, and bank details on a separate page.
